(setq elmo-imap4-debug nil)

; フォルダ名が省略されないように現在のユーザ名を使わないで、ダミーの名前にしておく。
(setq elmo-imap4-default-user "imap4-default-user")

(setq elmo-imap4-use-modified-utf7 t)

;;; Local Variables:
;;; mode: emacs-lisp
;;; coding: utf-8
;;; indent-tabs-mode: nil
;;; End:
